# docker-updater
A Python script that automates the update of container images for Docker and Podman Compose projects. It fetches the latest SHA256 digest from a configurable API, intelligently updates the YAML file while preserving comments, and automatically redeploys containers to make your latest truly latest.
